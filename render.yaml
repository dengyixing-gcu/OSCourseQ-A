services:
  - type: web
    name: oscourseq-a-proxy
    env: python
    rootDir: proxy
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: ZHIPU_API_KEY
        sync: false
      - key: PORT
        value: 10000
